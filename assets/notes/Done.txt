Fixed:
- fix(settings): remove .0 or .5 trailing decimals from legacy integer slider preferences
  - Modified Appearance, Visualizer, Playback, and General settings menus to enforce clean integer strings in dialogs

- fix(database): resolve Room Binder crash on saving player queue with large custom covers
  - Updated `Converters.kt` to purge `artworkData` prior to Room serialization
  - Only runs when `artworkData` is present to prevent unnecessary GC pauses

- fix(playback): ensure StatsForNerds streamClient data persists across app cache wipes
  - Created `PlaybackDataStore.kt` for persistent storage
  - Updated `StreamResolver.kt` and `PlayerServiceModern.kt` to leverage DataStore instead of temporary cache

- fix(playback): populate missing Format fields (Size, Loudness) for downloaded songs in StatsForNerds
  - Added `fetchFormatIfMissing()` in outer `ResolvingDataSource`
  - Performs a HEAD request and uses `playerResponseForMetadata` if fields are null/incomplete, then upserts the merged result into Room DB

- fix(ui): standardize magnetic snapping thresholds for slider controls
  - Replaced legacy `CustomSlider` with a universal `SliderControl` architecture across the app
  - Snapping thresholds are now dynamically size-based to feel natural on touch screens
  - Re-enabled `stepSize` across appearance, player, visualizer, and volume settings

- fix(export & metadata): unify metadata reading and writing using FFmpeg and FFprobe exclusively
  - Replaced legacy `jaudiotagger` and `isoparser` implementations with robust FFmpeg/FFprobe commands for both `EditMetadataDialog.kt` and `ExportCacheDialog.kt`
  - Fixed "Publisher" mapping (℗) in YouTube descriptions to map to standard "Copyright" tag for broader compatibility
  - Cleaned up temp files securely using `finally` blocks
  - Standardized all tags including custom `EXPORTED="N-Zik"` tag

- fix(export): correct output file extension based on actual codec detection
  - `ExportCacheDialog.kt` now always uses `.ogg` extension when codec is Opus, regardless of DB mimeType match
  - Prevents FFmpeg from muxing Opus into M4A container which caused export failures

- fix(stats): display Container, Codec, Sample Rate, and Channels for local songs in StatsForNerds
  - Added `MediaExtractor` probe in `OnDeviceMedia.kt` to extract `sampleRate`, `audioChannels`, and `codec` during local file scan
  - `getCodec()` now prefers `format.codecs` field before falling back to mimeType parsing
  - Removed `LOCAL_KEY_PREFIX` guards for these fields in both overlay and modern player views

Changed:
- change(ui): set Theme morphM3 as the default animated gradient in appearance settings
- change(settings): moved Blur Effect out of Audio Effects and into Player Effects in the popup menu

Added:
- feat(visualizer): add direct toggle for Visualizer button from the Player Action Bar Settings Dialog

Improved:
- improve(visualizer): upgrade line thickness slider
  - Now uses the full `SliderSettingsEntry` component with chevron and dialog for better UX

Removed:
- remove(player): drop shake-to-switch music feature
  - Deleted sensor listener, settings UI, `ShakeSensitivity` enum, and related string resources

Fixed:
- fix(ui): align action bar and filter buttons with song items in Songs/Albums/Artists/Playlists screens
  - Reduced TabHeader start padding from 24dp to 12dp to match QuickPicks alignment
  - Removed extra Spacer(12.dp) from ButtonsRow to fix filter chips left padding
  - Now title, toolbar, and filter chips all start at 12dp like QuickPicks

Added:
- add(player): add visualizer and lyrics mutual exclusivity logic and properly sync state across the MiniPlayer
  - Added LaunchedEffect in Player.kt so changing one state correctly unchecks the other when shouldRemember is false
  - Added DisposableEffect to reset volatile lyrics/visualizer states when the player is fully closed

- add(settings): add default MiniPlayer button order configuration
  - Removed unused `expand_player` toggle
  - Synced default list in MiniPlayer.kt with MiniPlayerButtonsSettingsDialog.kt so factory reset matches initial install state

- fix(volume): synchronize in-app sliders with Android system volume in real-time
  - Implemented a BroadcastReceiver listening to VOLUME_CHANGED_ACTION in PlaybackSettingsMenu and AudioOutputMenu
  - Passed FLAG_SHOW_UI to audioManager.setStreamVolume to invoke the native Android volume bar when dragging sliders

- improve(ui): unify volume slider design across AudioOutputMenu and PlaybackSettingsMenu
  - Extracted ListSliderMenuItem to mimic VolumeRow exact proportions, paddings, and alignment
  - Updated icons using accent colors inside a 32dp rounded Box with 0.1f alpha
  - Restored SliderGridEntry to preserve grid layout in PlaybackSettingsMenu while adhering to the new 32dp icon design

- improve(ui): dynamic MiniPlayer audio output icon matching active external device (bluetooth, headphones, etc) and accent color
- fix(playback): fallback to active media device correctly when no manual routing is set, preventing Earpiece from showing as active
- improve(ui): add basicMarquee to long text fields in PlaybackSettingsMenu to prevent layout breaking on languages with long translations
- refactor(playback): centralize AudioDevice icon mapping in AudioOutputManager and clean up redundant UI logic in MiniPlayer

Changed:
- change(settings): QuickPicks dialog — remove 'Enable Quick Picks page' toggle from QuickPicksContentSettingsDialog
  - The setting is now only accessible from AIRecommendationSettings, not from the QuickPicks content dialog

- change(settings): QuickPicks dialog — pin 'Tips' section to top as non-reorderable fixed item
  - `parseOrder()` now forces 'tips' to index 0 regardless of saved order
  - `pinnedItemCount = 1` prevents it from being dragged

Fixed:
- fix(home): respect showTips preference in HomeQuickPicks — QuickPicksHeader + QuickPicksGrid now hidden when Tips is disabled
  - Wrapped the entire tips block (header + recommendations grid) in `if (showTips)`
  - Moved `showTips` declaration before the `loadedQuickPicks` check to fix 'Unresolved reference' compile error

Improved:
- improve(ui): reorderable settings dialogs (PlayerActionBar, MiniPlayerButtons, StreamClients, QuickPicksContent)
  - Refactored ToggleListDialog to support pinned items, locked-checked IDs, and per-item checked state overrides
  - New HomeAlbums, HomeArtists, HomePlaylists, HomeSongs settings dialogs added with drag-to-reorder support
  - Removed LibraryVisibilitySettingsDialog (consolidated into per-screen dialogs)

Removed:
- remove(settings): drop LibraryVisibilitySettingsDialog
  - Functionality replaced by individual HomeAlbums/HomeArtists/HomePlaylists/HomeSongs dialogs