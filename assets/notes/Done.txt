## 2026-08-12
Fixed:
- fix(playback): improve car detection icon and UI behavior
  - Added `isCar(name)` helper in `AudioDeviceUtils.kt` to identify car-related keywords in device names
  - Updated `getAudioDeviceIcon` and `getBottomSheetDeviceIcon` to prioritize car icon for recognized BT/USB devices
  - Integrated `CarConnection` API in `AudioOutputManager.kt` and `AudioDeviceMenu.kt` to detect Android Auto projection state
  - Fixed `MiniPlayer.kt` to allow car icon display even when active route is a built-in speaker/earpiece
  - Renamed active phone speaker to "Android Auto" and disabled source switching when projection is active to prevent disconnection
  - Blocked volume control in `AudioDeviceMenu.kt` during Android Auto sessions to prevent software conflicts
  - Added fluid transitions with `Crossfade` and `animateContentSize` in `AudioDeviceMenu.kt` for a smoother UI experience

- Fixed Android Auto infinite loading by making getLocalSongs a cold flow and doing codec extraction asynchronously.
- Fixed empty album art in Android Auto by properly checking artwork existence and assigning fallback drawable for all songs.
