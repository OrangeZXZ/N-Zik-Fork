- Fix Home Page loading for anonymous users
- Fix "stuck" data issue by removing disk persistence for dynamic YouTube sections
- Reset session and cache on login/logout
- Refactor Discover feature: update Radio/Discover behavior logic, fix string states, standardise Discover button UI (ActionBar & Queue) to match settings and fix visual padding/alignment
- fix login for radio, discover, autofill.
- Centralize all shuffle operations into Shuffler.kt + add toast notifications with item count for all user-initiated shuffles
- ArtistDetails: fetch all songs for global shuffle/play-next/enqueue instead of only top 5 + add loading indicators on dice and global toolbar
- BetterLyrics lyrics overhaul: parse agent/bg tags for chat alignment, force re-fetch on mode switch (Karaoke/Sync/Unsync), keep existing synced when karaoke not found, cleanPrefix on artistName, toast for all lyrics sources
- Fix explicit badge not showing when playing from Android Auto (preserve existing extras in MediaItemMapper.mapSongToMediaItem)
- Fix Android Auto tabs order to match app predefined order (Songs: All/Favorites/Cached/Downloaded/Top/OnDevice, Artists: Favorites/Library, Albums: Favorites/Library, Playlists: Local/Pinned/Monthly/Piped/YT)
- Fix Android Auto sort order to match user preferences for all sections (Songs, Artists, Albums, Playlists)
- Fix Android Auto playlist songs sort order to match user preferences per playlist (PlaylistSongsSortBy_$playlistId)
- Optimize Android Auto artwork sizes: 1200px for main player and 250px for library lists (artists, albums, songs) to reduce data/RAM usage
- Fix Android Auto album tracklist to fetch from network first (for complete lists) before falling back to local DB
- Update local database metadata when fetching albums in Android Auto (similar to HomeAlbum behavior) to fix missing or broken artwork and details
- Added YouTube Music Quick Picks to Android Auto ID_QUICK_PICKS and removed the fixed limit by fetching up to 500 trending items directly from the database instead of using the restrictive QuickPicksRepository limit.
- Split Quick Picks on Android Auto (Local vs YouTube) and added Timber logs for debugging.
- Grouped Quick Picks sources on Android Auto into a single list.- Implemented virtual pagination for Android Auto folders exceeding 200 items to bypass the 1000 items limitation and Binder IPC limit.
- Added 'Play All' and 'Shuffle' buttons to the top of all Android Auto folders (Songs, Artists, Albums, Playlists).
- Fixed 'Play All' and 'Shuffle' sorting behavior in Android Auto to strictly respect the user's custom sort preferences instead of defaulting to database fetch order.
- ANDROID AUTO If a playlist exceed 200 music the app will create subfolder by page to avoid Android auto missing songs
- ANDROID AUTO Refactor artist browse tree to mirror ArtistDetails: dynamic sections from YtMusic.getArtistPage() instead of hardcoded ALBUMS/SINGLES/VIDEOS/SONGS — handles all section types (Songs, Albums, Singles & EPs, Videos, Playlists, Fans might also like) with proper moreEndpoint browse handling for each renderer type (grid, carousel, shelf, playlistShelf). Fixes #498.