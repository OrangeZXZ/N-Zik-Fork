Added: Real-time audio wave visualization in the seekbar
Fixed: Concurrent execution with NextVisualizer by implementing a shared visualizer system
Added: True static waveform timeline extraction and display based on MediaExtractor (for cached songs)
Removed: Deprecated "Fake Audio Bar" logic, fully replaced with dynamic and static Audio Waves
Added: Automatic cleanup for waveform cache when clearing song downloads, offline cache, or listen history
Improved: Static waveform visualization aesthetics (real & fake) with dynamic amplitudes and removed unnecessary extraction logs
Fixed: App crash on background playback due to ForegroundServiceStartNotAllowedException in PlayerServiceModern
Fixed: IllegalArgumentException crash in SeekBarStaticAudioWaves on Canvas initialization with zero dimensions
Added: Flat line animation when paused/stopped and current position thumb indicator for static timelines
Improved: Seekbars properly handle transparent mode background colors
Fixed: Reverted GetSeekBarType.kt to use binder.player.isPlaying and introduced internal isPlayerPlaying DisposableEffect inside SeekBarStaticAudioWaves to accurately track ExoPlayer playback state independent of parent recomposition.
Fixed: Fixed race condition in SeekBarStaticAudioWaves where isPlaying state transition was missed if ExoPlayer started instantly from cache
Fixed: Fixed visualizer bar (SeekBarVisualizer) stuck as flat line on cached songs by adding internal ExoPlayer listener instead of relying on stale parent state
Fixed: Fixed stale closure bug in SeekBarVisualizer by preserving MutableState instance across recompositions on layout changes

Refactored: Extracted nextvisualizer into an independent git submodule, flattened its structure and removed its R class dependencies
Refactored: Renamed the main application module from composeApp to ComposeN-Zik
Fixed: Widespread bug where combinedClickable on IconButton intercepted clicks and made songs unclickable across the app
Fixed: Layout bug where minimumInteractiveComponentSize on inline indicator icons (like explicit "E") forced a 48dp size, crushing the UI in song items
Fixed: Swapped inverted icons in DownloadAllSongsDialog and DeleteAllDownloadedSongsDialog
