feat(updater): add OTA support for dev builds
- Added dev channel to Updater (SUFFIX_DEV, TYPE_DEV, CHANGELOG_DEV)
- Enabled IS_AUTOUPDATE for dev/dev32 build types
- findBestRelease: dev builds only accept dev releases
- isVersionNewer: compare date suffix for dev tags (v7.3.2-dev-20260806)
- ChangelogCard: Dev section with sparkles icon and yellow dot
- build-dev.yml: copy Updater/changelogs/dev.txt into APK
- Add Updater/changelogs/dev.txt with generic dev message and report links

BETA TAG CHANGE:
- Changed beta release tag from `-b` to `-beta` (e.g. `v7.3.2-beta` instead of `v7.3.2-b`)
  - Updated GitHub Actions workflow (build-beta-flavor.yaml): tag_name, release name, download URLs
  - Updated weekly workflow (build-all-flavors-weekly.yml): beta tag check
  - Updated Gradle build types: versionNameSuffix `-b` → `-beta`, `-b32` → `-beta32`
  - Updated UpdaterConstants.kt: SUFFIX_BETA = "-beta", SUFFIX_CHAR_BETA = "beta"
  - Updated Skeleton.kt: default beta preference check

Metrolist Parity Sync (cipher completion):
- feat(cipher): complete Metrolist cipher parity — all 8 cipher files now synced
  - FunctionNameExtractor: expression-based sig now propagates all config fields (constantArg, constantArgs, preprocessFunc, preprocessArgs) in a single SigFunctionInfo return, matching Metrolist's single-return pattern
  - PlayerConfigStore: added 3 test helpers (armForcedCooldownForTest, armRejectionCooldownForTest, setTableForTest) for unit testing cooldowns and config injection
  - RendererRecoveryPolicy: constructor now parameterizable with maxConsecutiveFailures and backoffMs defaults (backed by companion object constants); backoffUntilMs exposed as public var for CipherDeobfuscator logging
  - CipherDeobfuscator: rendererRecoveryPolicy.onSuccess() now called inside transformNInternal() after successful n-transform (was in transformNParamInUrl), matching Metrolist's placement exactly — ensures policy resets on n-transform-only devices
  - Full line-by-line comparison: 0 meaningful gaps remaining across all 8 cipher files, ContentAwareFallbackStrategy, and YouTubeClient

Metrolist Parity Sync:

Added:
- feat(cipher): port Metrolist cipher pipeline improvements
  - CipherWebView: moved player.js heavy processing (2.8MB string transform + disk write) off Main thread to Dispatchers.IO, preventing UI freezes during WebView rebuilds
  - CipherWebView: added NonCancellable context in destroyQuietly to ensure cleanup completes even on cancellation
  - FunctionNameExtractor: safer STS precedence — anchored `signatureTimestamp` literal first (immune to config typos), then config, then loose `sts` pattern
  - FunctionNameExtractor: added match context logging and hash lookup debug logging
  - PlayerJsFetcher: invalidateCache now only deletes player_* files, preserving configs_remote.json and its ETag
  - PlayerJsFetcher: added cacheWriteLock synchronized to prevent concurrent cache write corruption
  - PlayerJsFetcher: HTTP responses wrapped in .use{} to prevent connection leaks on error paths
  - PlayerJsFetcher: withinWindow() for NTP-safe TTL checks instead of raw subtraction
  - PlayerJsFetcher: writeAtomic for cache writes to prevent truncation on process death
  - PlayerConfigStore: NPE fix — response.body?.string() instead of response.body.string()
  - PlayerConfigStore: writeAtomic adds target-delete before rename for filesystem compatibility
  - PlayerConfigStore/PlayerDatesStore: REMOTE_URL changed from MetrolistGroup/Metrolist to ZemerTeam/zemer-cipher (configs up to sts 20669)
  - Updated bundled player_configs.json from ZemerTeam/zemer-cipher (includes ea6f527e)

- feat(stream clients): add ANDROID_VR_1_65_10 and TVHTML5_SIMPLY clients
  - Added ANDROID_VR_1_65_10 client definition (newer Android VR version, Android 12L)
  - Added TVHTML5_SIMPLY client definition (lightweight TV variant)
  - Added streamClientTvSimplyEnabledKey preference key
  - Added TV Simply toggle in StreamClientsSettingsDialog
  - Added TVHTML5_SIMPLY disable logic in StreamResolver
  - Added ANDROID_VR_1_65_10 and TVHTML5_SIMPLY to FALLBACK_CLIENTS
  - Added TVHTML5_SIMPLY to WEB_CLIENTS set
  - Added "TV Simply" string resources (name + description)

- feat(content-aware): add ContentAwareFallbackStrategy
  - New file with ContentHints data class and 5 content-specific client lists (default, uploaded, live, kids, explicit)
  - Uses ANDROID_VR_1_65_10 and TVHTML5_SIMPLY in default list
  - Integrated into StreamResolver for content-aware client ordering

- feat(settings): wire all client descriptions in StreamClientsSettingsDialog
  - All 13 stream clients now show descriptions in the settings dialog
  - Added client_tv_simply_desc string resource

- feat(error handling): per-song error classification in PlayerServiceModern
  - Added isPageReloadError, isRemotePlaybackError, isNetworkRelatedError classifiers
  - MAX_RETRY_PER_SONG=3 with recentlyFailedSongs tracking
  - Retry delays: 1s default, 3s audio renderer, 2s page reload
  - Seek to 0 for 416/file-not-found errors
  - isRemotePlaybackError mapped to expired URL handler with cipher refresh
  - performAggressiveCacheClear includes songUrlCache.remove()

- feat(download): LRU URL cache and cache-first downloads
  - songUrlCache changed to LRU LinkedHashMap (max 500 entries)
  - Download factory checks downloadCache.isCached() before network
  - ContentAwareFallbackStrategy integrated for client reordering
  - Download 403 retry with URL cache invalidation
  - clearStreamCaches includes songUrlCache.clear()

Added:
- feat(stats): add scroll support and live data refresh to StatsForNerds overlay
  - Added `clipToBounds()` and `nestedScroll` to prevent scroll gestures from propagating to parent and closing the player
  - Replaced `collectAsState` with `LaunchedEffect` polling every 1s for `playbackDataCache`, `binder.cache`, `binder.downloadCache`, and `Database.formatTable`
  - StatsForNerds now auto-refreshes when stream client or link changes during playback
- feat(sort): change default playlist sort from SongCount to Name
  - Updated `HOME_LIBRARY_*_SORT_BY` defaults in `Preference.kt` to `PlaylistSortBy.Name`
- feat(sort): change artist/album sort labels to "Name" for clarity
  - Changed `ArtistSortBy.Name` textId from `R.string.sort_artist` to `R.string.sort_name`
  - Changed `AlbumSortBy.Title` textId from `R.string.sort_album` to `R.string.sort_name`
  - Updated `HomeArtistsSortSettingsDialog` and `HomeAlbumsSortSettingsDialog` getSortLabel accordingly

Fixed:
- fix(stats): remove format "complete" skip in StreamResolver to ensure fresh data on client change
  - `fetchFormatIfMissing` no longer skips re-fetch when format exists and is "complete" in Room DB
  - Ensures codec/bitrate/sample-rate data updates when stream client changes

feat(sort settings): add sort menu customization and Local Playlist toolbar
- feat(sort settings): add sort menu customization dialogs for all screens
  - Created HomeSongsSortSettingsDialog (6 tabs), HomeAlbumsSortSettingsDialog (2 tabs), HomeArtistsSortSettingsDialog (2 tabs), HomeLibrarySortSettingsDialog (4 tabs), HistorySortSettingsDialog, LocalPlaylistSortSettingsDialog
  - All dialogs reuse exact same string resources (textId) and drawable resources (iconId) from their corresponding sort enums
  - Users can reorder and hide sort options per screen, minimum 1 visible enforced
- feat(sort settings): integrate sort settings into UiSettings screen
  - Added "Sort Menu" section with 6 entries (Songs, Artists, Albums, Playlists, History, Local Playlist)
  - All dialogs .Render() and .reset() wired in UiSettings.kt
- feat(toolbar settings): add LocalPlaylist toolbar customization
  - Created LocalPlaylistToolbarSettingsDialog with 21 buttons (sort, shuffle, search, position_lock, match, download_all, delete_downloads, item_selector, play_next, enqueue, add_to_favorite, add_to_playlist, sync, listen_on_yt, import_menu, rename, delete, export, thumbnail_picker, reset_thumbnail, reset_cache)
  - Added "Local Playlist" entry in Toolbar section of UiSettings
  - Modified LocalPlaylistSongs.kt to read saved toolbar order from preferences
- feat(sort): wire sort menu order keys into all Sort instances across home screens
  - Added `sortMenuOrderKey` and `sortMenuPrefix` parameters to Sort.kt constructor
  - Updated HomeSongs, HomeSongsScreen, HomeAlbum, HomeArtist, HomeLibrary, OnDeviceSongs, PlaylistSongsSort to pass per-tab sort menu preferences
  - Sort menu now reads saved order from SharedPreferences and filters hidden options
- feat(thumbnail): always show overlay on album/artist/playlist thumbnails
  - Removed `if (count > 0)` checks — overlay now displays even when play count or listening time is 0
  - Added SongCount overlay for playlists in HomeLibrary
- feat(ui): improve ToggleListDialog drag handle and row layout
  - Increased drag icon size to 24.dp with 4.dp padding for better touch target
  - Restructured row layout: inner clickable Row wraps icon+label+checkbox, outer Row adds drag handle
- fix(sort): fix Sort.kt Kotlin 2.4 generic type inference for enum reflection
  - Added explicit type annotation `Map<String, T>` for enumMap
  - Added missing `import androidx.compose.runtime.remember`
- fix(history): restore missing closing brace in HistoryList.kt
- fix(sort): remove unused `sort_date_modified` string reference (not in any enum)
- fix(database): fix PlaylistTable queries to include empty playlists
  - Changed JOIN to LEFT JOIN in `sortPreviewsByListeningTime` and `sortPreviewsByPlayCount`
  - Added COALESCE for null sums so playlists with no songs return 0 instead of being excluded

Fixed:
- fix(sort): fix 34 inverted SQL sort queries across 6 database tables
  - All sort methods for PlayCount, PlayTime, RelativePlayTime, DatePlayed, SongsCount, ListeningTime, Duration now correctly return ASC order instead of DESC
  - Affected tables: SongTable, SongPlaylistMapTable, AlbumTable, ArtistTable, PlaylistTable, FormatTable
  - Root cause: methods used `sortedByDescending` or `ORDER BY ... DESC` but `SortOrder.applyTo()` assumes ASC input, causing user's sort direction to be inverted
- fix(sort): fix HistoryList.kt DATE sort double-reversal bug
  - Removed extra `.reversed()` that was canceling the sort direction for DATE history sorting
- fix(sort): fix OnDeviceSongSortBy.DateAdded wrong label (was showing "Date Played" instead of "Date Added")
- fix(sort): align Android Auto defaults with Preference.kt
  - Fixed TOP/ALL/FAVORITES/DOWNLOADED defaults from mismatched values to correct Title+Ascending
  - Replaced in-memory sorts with BDD sort methods in SongsBrowseHandler, PlaylistsBrowseHandler, PlaylistDetailHandler, AutoSessionCallback
- fix(sort): replace old-style preference keys in PlayerMenu.kt
  - Changed `playlistSortByKey`/`playlistSortOrderKey` to `Preference.HOME_LIBRARY_PLAYLIST_SORT_BY`/`HOME_LIBRARY_PLAYLIST_SORT_ORDER`
- fix(favorites): change default favorites sort from Title to DateLiked+Ascending
- feat(sort): reorder enum declarations for logical menu grouping
  - All sort enums now group by type: Text → Numbers → Dates → Other
  - SongSortBy, PlaylistSongSortBy, AlbumSortBy, PlaylistSortBy, ArtistSortBy
- feat(sort): add visual highlight for selected sort option in sort menu
  - Currently selected option now shows accent color background (alpha 0.2f on icon, 0.1f on row)
- feat(thumbnail): add overlay background to StatisticsPage and AlbumDetails track numbers
- fix(deps): extract FilterMenu from deleted SortMenu.kt into new FilterMenu.kt
  - Updated imports in HomeAlbum.kt and HomeArtist.kt to use new location

Improvement:
- Improved app header (added 32-bit tags and replaced "debug enabled" message with an elegant tag).

Fixed:
- fix(updater): apply infinite marquee to handle long translation texts and missing center alignment
  - Applied `basicMarquee(iterations = Int.MAX_VALUE)` to all texts in `UpdateScreen.kt` that can overflow, including app versions, download statuses, and buttons
  - Restored horizontal centering for texts modified by the marquee by using `TextAlign.Center` directly in the `TextStyle`

BETA V 7.3.2
Added:
- feat(settings): implement search functionality across all settings screens
  - Integrated search.ToolBarButton() and search.SearchBar(this) in Data, Network, Appearance, Other, Accounts, UI, General, and AIRecommendation settings.
  - Placed the search bar uniformly below the page description.
- feat(settings): add universal Reset to Default feature for all individual settings screens
  - Created centralized Default...Settings() functions for each screen that properly initialize default states without duplicating variable instantiation.
  - Included a Reset block that clears preferences (excluding sensitive info like cookies/tokens) and calls reset(context) on associated dialogs.
- style(settings): cleanup and standardize imports across all settings files
  - Removed fully qualified inline packages in favor of proper top-level imports to adhere to project conventions.
- fix(settings): sync toolbar settings dialog defaults with actual screen defaults
  - Replaced hardcoded default lists in HomeSongsScreen, HomeAlbum, HomeArtist, and HomeLibrary with references to their respective SettingsDialogs (`allButtonIds` and `tabAvailableIds`)
  - Ensures all toolbar buttons (like downloads or match) are correctly displayed upon a fresh install without needing to trigger a reset
  - Root cause: toggles were writing directly to SharedPreferences on every click, so Cancel still persisted the changes in memory
  - Introduced `workingToggles` local state (Map/List) in all toolbar and section settings dialogs, only committing to SharedPreferences on Confirm
  - Applied to: HomeSongsToolbarSettingsDialog, HomeAlbumsToolbarSettingsDialog, HomeArtistsToolbarSettingsDialog, HomeLibraryToolbarSettingsDialog, HomeSongsSettingsDialog, HomeAlbumsSettingsDialog, HomeArtistsSettingsDialog, HomePlaylistsSettingsDialog
- feat(settings): add `reset(context)` function to all settings dialogs for programmatic full reset
  - Each dialog can now reset its order and toggle preferences to factory defaults via a single call
  - Added to: HomeSongsSettingsDialog, HomeAlbumsSettingsDialog, HomeArtistsSettingsDialog, HomePlaylistsSettingsDialog, HomeSongsToolbarSettingsDialog, HomeAlbumsToolbarSettingsDialog, HomeArtistsToolbarSettingsDialog, HomeLibraryToolbarSettingsDialog, PlayerActionBarSettingsDialog, StreamClientsSettingsDialog, PreferredStreamClientDialog
- fix(download): prevent missing artist/album background fetches from blocking downloads and crashing the app
  - Added `autoFix` parameter to `Database.insertIgnore` (defaulting to true) and set it to false in `MyDownloadHelper.kt` to disable background fetches that cause downloads to randomly fail
  - Moved the Database fallback query for missing artist names (`mediaItem.artistTextOrDb()`) inside a background coroutine to resolve a `NetworkOnMainThreadException` crash on the UI thread
  - Refactored notification title generation to elegantly handle missing artist and/or title metadata (defaulting to `unknown_title`)
  - Safely fallback to `unknown_title` and `unknown_artist` in `Player`, `MiniPlayer`, and `SongItem` without modifying database state to preserve Discord RPC functionality
  - Updated `Modern.kt` player to dynamically display `unknown.xml` icon when title or artist strings match the unknown fallbacks

BETA V 7.3.1
Added:
- fix(toolbar-settings): fix toggle state shared across tabs in Albums, Artists, and Library toolbar settings dialogs
  - Root cause: `getTabPrefix()` switch cases ("favorites", "pinned", "monthly") didn't match the actual tab keys ("favs", "pin", "mon"), so multiple tabs mapped to the same prefix → same preference key → shared checkbox state
  - Fixed `getTabPrefix` in HomeAlbums, HomeArtists, and HomeLibrary dialogs to match actual tab keys
- fix(toolbar-settings): prefix LazyColumn item IDs per tab in all toolbar settings dialogs (Songs, Albums, Artists, Library)
  - Root cause: LazyColumn keys were bare IDs (e.g. "sort"), identical across tabs; Compose reused composites so `rememberPreference` never re-read the new tab's preference key
  - Each dialog now prefixes item IDs with a tab-specific prefix (e.g. "all_sort", "favs_sort") to force unique composites per tab
- fix(songs-toolbar): remove forced download/export/delete buttons that couldn't be toggled off
  - Removed `LaunchedEffect` in `HomeSongs.kt` that unconditionally re-added `downloadAllDialog`, `deleteDownloadsDialog`, and `exportDialog` after every recomposition, ignoring user preferences
  - These buttons are now fully controlled by the saved toolbar order, like all other buttons
- fix(songs): fix duplicate sort button on OnDevice tab
  - `OnDeviceSongs.kt` force-added `odSort` at position 0 via `LaunchedEffect(Unit)`, while the `remember` block in `HomeSongsScreen.kt` also added `songSort` from the saved order
  - Removed the force-add from `OnDeviceSongs.kt` and added `BuiltInPlaylist.OnDevice` case to `songSort` in `HomeSongsScreen.kt` using the correct `HOME_ON_DEVICE_SONGS_SORT_*` preferences
- feat(library): allow editing artist and album browse ID to fix incorrect auto-matches
  - Upgraded `ChangeArtistBrowseIdDialog` and `ChangeAlbumBrowseIdDialog` into online search pickers allowing users to search and select correct IDs.
  - Added "Unlink" (creates a LOCAL_ ID to prevent auto-match) and "Reset" buttons to easily correct manual overrides.
  - Updated network handlers (`UpdateYoutubeEntity`, `AlbumDetailHandler`, `ArtistDetailHandler`, etc.) to correctly strip the `M~` prefix and skip network calls for `LOCAL_` entities.
  - Integrated into `LocalArtistItemMenu`, `OnlineArtistItemMenu`, `AlbumItemMenu`, and `OnlineAlbumItemMenu`
  - Added `updateArtistId` and `updateAlbumId` in database transactions to correctly update ID maps and replace the old entity
- feat(build): introduce 32-bit compatible builds (`full32`, `minified32`, `beta32`)
  - Drops FFmpeg to support older 32-bit devices (`armeabi-v7a`), Media Export is disabled on these builds
  - Updated GitHub CI/CD workflows to automatically assemble and release these new APKs
  - Ensured seamless OTA update support in the native Updater for the new variants

Removed:
- remove(piped): drop Piped integration entirely from the project
  - Deleted the entire `extensions/piped/` module and removed its inclusion from `settings.gradle.kts`
  - Removed all Piped session models and utils: `PipedResponse.kt`, `GetPipedSession.kt`, `SyncPipedUtils.kt`, `PipedSession.kt`
  - Removed Piped dependencies from `ComposeN-Zik` and `innertube` modules' `build.gradle.kts`
  - Removed all Piped login, sync, and playlist logic from UI components including `AccountsSettings`, `PlayerMenu`, `NewPlaylistDialog`, and `RenamePlaylistDialog`
  - Removed Piped toggle and filtering logic from `HomeLibrary`
  - Removed `Piped.fetchInstances()` initialization from `MainActivity`

Fixed:
- fix(ui): animate menus and dialogs sliding down when closed instead of disappearing instantly
  - Modified `CustomModalBottomSheet` to orchestrate the `sheetState.hide()` animation before removing itself from the Compose tree.
  - Removed the `key` wrapper around `rememberModalBottomSheetState` in `MainActivity.kt` to ensure the menu's state is preserved during its exit animation.
- fix(ui): fix "Change Browse ID" dialogs instantly closing upon clicking the menu action
  - The menu state was being hidden before the dialog could compose itself, causing the dialog to be destroyed immediately since it resided within the menu's composition context.
  - Moved the `menuState.hide()` calls into the `onConfirm` flow inside the dialogs (`ChangeArtistBrowseIdDialog`, `ChangeAlbumBrowseIdDialog`, `ChangePlaylistBrowseIdDialog`) so the menu closes at the same time as the dialog finishes, rather than preemptively.
- fix(ui): align HomeSongs empty state ("No items") and toolbar padding with HomeAlbum and HomeLibrary to maintain consistent UX across the app.
- improve(ui): hide YouTube sync button in HomeAlbum, HomeArtist, and HomeLibrary when YouTube sync is disabled or disconnected.
- fix(ui): make all `MenuIcon` actions (like Shuffle) automatically close the overflow/bottom menu when clicked
  - Fixed `SongShuffler` component when used within `ListMenuItem` to force the menu to close on click.
- fix(ui): revert automatic menu closing for `MenuIcon` to fix dialogs instantly unmounting when clicked.
- fix(ui): eliminate duplicate "Browse ID" text on the Edit ID menu options (now just says "Change ID").
- feat(library): add Shuffle button to Album and Artist contextual menus (both local and online).
- fix(ui): fix queue search bar being hidden by the bottom toolbar and software keyboard
  - Added bottom padding (`64.dp`) to the SearchBar in `Queue.kt` when it is visible so it sits above the `QueueToolBar`.
  - Added `Modifier.imePadding()` to ensure the search bar is pushed up correctly when the software keyboard opens.
- fix(playback): resolve auto-fix race conditions and missing UI updates
  - Fixed an issue where the background auto-fix would silently abort on the very first playback because it queried the database before the async song insertion transaction completed. Moved `insertIgnore(item)` into the same coroutine block for synchronous execution.
  - Fixed an issue where the "Go to Artist" and "Go to Album" buttons required restarting the music to function. Removed the Compose `remember` caching (`init` block) from `GoToArtist.kt` and `GoToAlbum.kt` so they always fetch the freshest ID from the database at the exact moment of click, eliminating the need to restart after an auto-fix.
- fix(playback): resolve database corruption/race condition on music playback initialization
  - Immediately persist complete `MediaItem` metadata to the database in `PlayerServiceModern.kt` during `onMediaItemTransition`.
  - Added a check in `StreamResolver.kt` to prevent inserting a blank placeholder `Song` if the song already exists in the database.
- fix(database): resolve Room transaction deadlocks by replacing runBlocking/Flow calls with synchronous DAO queries (`findByIdDirect`, `countById`) to prevent database freezes during playback.
- fix(database): resolve database lock caused by network calls inside Room transactions
  - Moved `Innertube.searchPage()` network calls outside `Database.upsert()` transaction block — network I/O was holding Room's write lock for seconds, blocking all other DB operations.
  - Replaced `runBlocking { artistTable.findByName().first() }` with synchronous `findByNameDirect()` in `Database.insertIgnore()` to prevent blocking the single-threaded transaction executor.
- fix(database): comprehensive database locking audit and fixes across 12 files
  - Added retry logic with exponential backoff to `Database.asyncTransaction` for `SQLiteDatabaseLockedException`.
  - Added synchronous DAO query methods across all tables (`findByIdDirect`, `findByNameDirect`, `isLikedDirect`, `findBySongIdDirect`, `findBySongIdDirect`, `allDirect`, `allSongsOfDirect`, `existsDirect`) to eliminate `runBlocking { Flow.first() }` patterns.
  - StreamResolver: replaced `runBlocking { Flow.firstOrNull() }` with direct `findByIdDirect()` / `findBySongIdDirect()` queries.
  - StreamResolver: converted `upsertSongInfo` to `suspend fun` to eliminate blocking wrapper.
  - PlayerServiceModern: replaced `runBlocking` in `maybeRestorePlayerQueue` / `maybeRestoreFromDiskPlayerQueue` with `allDirect()` + `CoroutineScope(Main).launch`.
  - PlayerServiceModern: merged duplicate `asyncTransaction` calls in `onPlaybackStatsReady` into a single transaction.
  - MediaItemUtils: replaced `runBlocking { Flow.first() }` in `artistTextOrDb()` / `albumTitleOrDb()` with direct synchronous queries.
  - Player.kt: replaced triple `runBlocking { Flow.first() }` with direct DAO queries.
  - HomeAlbum.kt: replaced `runBlocking { Flow.first() }` in Composable with `allSongsOfDirect()`.
  - MyDownloadHelper: replaced `runBlocking { Flow.first() }` with `isLikedDirect()`.
  - MatchAlbumUtils: replaced `Flow.first()` inside `withTransaction` with `findByIdDirect()`.
  - NZikRadio: replaced `Flow.first()` loops with `isMappedSync()` / `isLikedDirect()`.
  - SyncPipedUtils: replaced `runBlocking { Flow.first() }` inside `asyncTransaction` with `existsDirect()`.
  - SyncYTMusicUtils + LocalPlaylistSongs: moved network calls outside `asyncTransaction` blocks.
- fix(playback): eliminate remaining DB lock sources in crossfade and notification paths
  - StreamResolver `DataSpec.process()`: replaced `songTable.findById().firstOrNull()` Flow call with synchronous `findByIdDirect()` inside the `runBlocking(Dispatchers.IO)` block that ExoPlayer requires — this was the last Flow query in the critical stream resolution path triggered during crossfade.
  - PlayerServiceModern `updateCustomNotification()`: removed unnecessary `runBlocking` wrappers around `artistTextOrDb()` / `albumTitleOrDb()` which are now synchronous.
  - AutoSessionCallback `onGetItem()`: replaced `songTable.findById().first()` Flow call with synchronous `findByIdDirect()`.
  - Removed unused `runBlocking` and `flow.first` imports from PlayerServiceModern.kt and StreamResolver.kt.
- fix(database): convert Database.upsert() to suspend fun, eliminating runBlocking wrapper
  - Removed `runBlocking` wrapper from `Database.upsert(SongItem)` — this was the most dangerous pattern: `runBlocking` wrapping `withTransaction` + network calls, blocking the calling thread for the entire DB read + Innertube search + DB write duration. Now properly `suspend fun` so callers use coroutine context.
  - `Player.kt` `forcePlayAtIndex()`: replaced `runBlocking { upsertSongInfo() }` with direct suspend call (already inside `CoroutineScope(Dispatchers.Default).launch`).
  - `SongItem.kt` download click: moved `Database.upsert()` into `CoroutineScope(Dispatchers.IO).launch` to prevent main thread blocking.
  - `YouTubeSync.kt` `toggleSongLike()`: replaced `runBlocking { likeState().first() }` with direct suspend `.first()` call.
  - `PlaylistSongList.kt`: replaced `runBlocking { isLiked().first() }` in Composable with `LaunchedEffect` to prevent main thread blocking.
  - Removed unused `runBlocking` and `flow.first` imports from Database.kt, Player.kt, YouTubeSync.kt, PlaylistSongList.kt.
- fix(database): prevent permanent DB lock from unhandled exceptions during crossfade
  - `Database.asyncTransaction()`: added broad `catch (e: Exception)` around transaction execution — previously only `SQLiteDatabaseLockedException` was caught; any other exception (FK violation, NPE, etc.) would kill the transaction executor thread permanently, causing all subsequent `asyncTransaction` calls to queue forever.
  - `PlayerServiceModern.onPlaybackStatsReady()`: wrapped entire callback body in `try-catch(Exception)` to prevent exceptions from propagating and killing the transaction executor.
  - `PlayerServiceModern.onMediaItemTransition()`: wrapped `asyncTransaction { insertIgnore(it) }` in `try-catch(Exception)` to prevent exceptions during crossfade from killing the executor.
  - Removed unused `runBlocking` and `flow.first` imports from PlayerServiceModern.kt and AutoSessionCallback.kt.
- fix(playback): fix play time stats and history logging for songs played from albums/playlists
  - `PlaybackStatsListener` now correctly splits `mediaItem.mediaId` containing slashes (e.g. `albumId/videoId`) to extract the canonical `songId`, ensuring successful updates to the `Song` table and satisfying foreign key constraints for `Event` history entries.
- fix(search): fix unsafe fallback search in GoToAlbum
  - Removed the unsafe `firstOrNull()` fallback in `GoToAlbum.kt` which resulted in navigating to wrong albums 100% of the time. Now only navigates if there is a strict match on the song ID, and displays a toast error otherwise.
- fix(library): update Artists and Albums library queries to match Songs behavior
  - Changed Room queries in `ArtistTable.kt` and `AlbumTable.kt` so that 'All' library screens fetch based on whether at least one song is liked or played (totalPlayTimeMs >= 1) instead of requiring songs to be mapped to a playlist.
- fix(album): resolve missing album release year
  - Updated `Database.insertIgnore(mediaItem)` to merge new album metadata with existing DB records on song transitions instead of ignoring.
  - Added background resolution: if a song's album does not have a release year in the database, a lightweight background coroutine is launched to fetch the album page and populate the year, description, and share URL automatically.
- fix(playback): resolve DB stuck after crossfade — history and play stats stopped recording
  - `PlaybackStatsListener` was never added to the secondary crossfade player, so after `player = nextPlayer` swap, `onPlaybackStatsReady` never fired again — no events, no play time updates.
  - Added `PlaybackStatsListener` to secondary player in `preloadCrossfade()`.
  - Removed duplicate `PlaybackStatsListener` registration on main player (was registered twice causing double callbacks).

- refactor(easter-egg): replace Pacman and Snake mini-games with static unknown easter egg images

BETA V 7.2.9

Fixed:
- fix(lyrics): prevent getting stuck on Karaoke when switching to Synced
  - `LyricsDecisionMaker.kt` now strictly forces a refetch if the user explicitly switches to Synced mode but the local database only contains Karaoke lyrics
  - BetterLyrics result is silently rejected (falling back to LrcLib/KuGou) if the user requested Synced but BetterLyrics provides word-by-word Karaoke timings
- fix(lyrics): "Fetch lyrics again" in Auto mode no longer causes infinite shimmer
  - `resetGlobalAttemptForType(Auto)` was a no-op (`Auto -> {}`), global attempt IDs were never cleared so `evaluateFetchNeeds` never triggered a re-fetch
  - `onFetchLyricsAgain` did not reset `checkedLyrics*` flags or toggle `checkLyrics`, so `noLyricsFound` stayed false and the shimmer condition remained true forever

Improved:
- improve(lyrics): real-time UI state updates in settings menu
  - `actualLyricsType` and `isLyricsNotNull` are now dynamically evaluated in `LyricsSettingsMenu`, ensuring the menu properly updates if lyrics are fetched in the background while it's open
  - Added `AnimatedContent` crossfade to the "Current state: ..." text in the menu to prevent violent visual flashes when lyrics load
  - Auto mode properly retains loading Shimmer and informative fallback Toasts across different sources

Updated:
- chore(deps): bump androidx.work:work-runtime-ktx from 2.9.0 to 2.11.2 (#705)

BETA V 7.2.8

Added:
- feat(backup): add customizable backup intervals and UI modernization (#698)
  - Upgraded settings dialog in `AutoBackupSettingsBlock.kt` with `AnimatedVisibility` for cleaner UX
  - Replaced hardcoded hourly interval with options: hourly, daily, weekly, and custom

- feat(backup): allow custom retention limits for automatic backups (#698)
  - Implemented `maxBackups` limit in `BackupManager.kt` via `enforceRetentionPolicy`
  - Keeps only the N most recent backups and deletes older documents from the SAF tree
  - Integrated with the settings UI

- feat(backup): option to include YouTube and Discord credentials in settings export (#698)
  - Evaluates `includeYtb` and `includeDiscord` flags during CSV generation in `BackupManager.kt`

Fixed:
- fix(lyrics): correct fallback logic when requested lyric type is unavailable
  - `LyricsDecisionMaker.kt` now correctly falls back from Karaoke → Synced → Unsynced, instead of returning null
  - Fixed `needSyncedFetch` to properly trigger when a song only has unsynced lyrics but synced lyrics are requested
  - When switching lyrics modes in `LyricsFetcher.kt`, previous fetch attempts are cleared to force a re-fetch if needed
  - `LyricsTrackSelector.kt` now correctly saves manually selected lyrics as `Unsynced` if they only contain `plainLyrics`

Improved:
- improve(backup): comprehensive and tagged logging for the backup lifecycle (#698)
  - Replaced generic logging with `Timber.tag("AutoBackup")` across `BackupManager.kt` and `AutoBackupWorker.kt`
  - Added logging for settings count to verify credential inclusions during export
  - Removed logcat spam in `triggerOnChangeBackup` to prevent log floods on DB activity

- improve(backup): make Auto Backup Interval a master switch (#698)
  - Wrapped dependent backup settings (location, DB change, max backups) in `AnimatedVisibility` so they hide when interval is "Disabled"
  - Updated `BackupManager.kt` so `triggerOnChangeBackup` immediately returns if `PREF_INTERVAL` is disabled, ignoring `PREF_DB_CHANGE`

- improve(backup): verify backup location integrity at startup (#698)
  - Added `verifyBackupLocation` in `BackupManager.kt` called from `MainActivity.kt` during startup
  - Validates `PREF_URI` through the SAF content resolver; if the user deletes the folder externally, it clears the preference, disables the interval, and cancels the scheduled work to prevent silent failures

- improve(lyrics): state management during fetch process
  - `LyricsFetcher.kt` explicitly marks `onCheckedLrcUpdated(true)` and `onCheckedKugouUpdated(true)` alongside InnerTube checks to ensure UI state doesn't hang