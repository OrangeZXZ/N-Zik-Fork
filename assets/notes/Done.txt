## EditMetadataDialog - Metadata Editor for On-Device Songs
- New centralized metadata editor dialog for local songs (reads/writes directly to audio files)
- Uses jaudiotagger with Mp4FieldKey for proper MP4 atom reading (©day, ©wrt, cprt, etc.)
- MMR (MediaMetadataRetriever) fallback for non-MP4 files only
- Logical field ordering: Cover, Title, Artist, Album, Genre, Year, Track, Disc, Album Artist, Composer, Copyright, Comment, Encoder, BPM, ISRC, Publisher, Lyricist, Conductor, Remixer
- Compact cover art layout (56dp image + pick button)
- All standard fields shown even if empty (user can fill in missing metadata)
- Translated field labels via string resources
- UI-only sorting with derivedStateOf (filled fields first by displayOrder, empty last)
- Fixed genre sorting: case-insensitive match via labelFromKey(rawId).uppercase()
- Fixed IndexOutOfBoundsException: indexOfFirst with stable key instead of indexOf

## Delete Button for Local Songs
- Hidden for local songs in both SongItemMenu and PlayerItemMenu
- Still available for remote/online songs

## PlayerItemMenu - Fixed & Refactored
- Removed extra closing brace that caused build failure
- Added missing imports (DeleteSongDialog, ExportCacheDialog, Optional, InProgressDialog)
- Added dialog definitions and Render() calls
- Refetch/Update button moved to Management section for remote songs

## Pull-to-Refresh in OnDeviceSongs
- Added AppPullToRefreshBox wrapping LazyColumn
- refreshKey triggers re-collection of songs

## Other Fixes
- Artist "modified:" prefix stripped in Player, PlayerServiceModern, AutoMediaItemMapper, MediaItemUtils
- Explicit checkbox reset in RenameSongDialog via LaunchedEffect
- Custom covers in AA via ArtworkContentProvider
- On-device artwork in AA via mapSongToMediaItem
- Gradient background stuck colors fixed in Player.kt
- EXIF rotation + center crop for cover images
- Coil cache cleared on cover change in ChangeCoverDialog
- ContentScale.Crop for custom images in SongItem and MiniPlayer
- CurrentMediaItemIdAsState reactive helper for on-device tab selection

## Dialog Reorganization
- Moved all dialog files from scattered locations to components/dialog/ with organized subdirectories
- Created 14 subdirectories: album/, artist/, backup/, common/, export/, logs/, media/, player/, playlist/, search/, settings/, song/, tab/, updater/
- Total: 52 dialog files organized
- Updated all package declarations to match new locations
- Updated all imports across the entire codebase (100+ files)
- Renamed "import" folder to "backup" to avoid Kotlin keyword conflict

## Quality Logging Enhancement
- Added image quality tracking in PlayerServiceModern (imageQualityFormat property + preference listener)
- Enhanced onMediaItemTransition log to show Network bandwidth, Audio quality, and Image quality
- Log shows effective quality when Auto is selected (e.g., "Auto→HIGH")
- Uses Info level (NZik_Network tag) for visibility in logcat

## Metrolist Comparison Analysis
- Stream: Quasi-identical (14 clients, PoToken, cipher, self-healing). NZik has Medium preset extra.
- Download: Equivalent architecture (ExoDownloadService, dual cache, auto-download on like)
- Dependencies: Aligned and up-to-date. NZik uses newer versions (Kotlin 2.4.0, Ktor 3.5.1, Coil 3.5.0)
- Parental Control: Functional but different approach (NZik uses title prefix vs Metrolist uses explicit boolean from API badge)

## Stream/Cipher Architecture Alignment with Metrolist
- Enhanced StreamResolver with improved client fallback chain (14 clients)
- Added PlaybackData cache for metadata access (loudness, videoDetails, tracking)
- Added HIGH-quality best-fallback tracking across clients
- Improved age-restriction handling with early WEB_CREATOR skip
- Added stream client settings dialog for enabling/disabling individual clients
- Added PlayerConfigStore with bundled + remote player_configs.json
- Added PlayerConfigParser for extracting sig/n functions from player.js
- Added RendererRecoveryPolicy for WebView renderer OOM handling
- Enhanced CipherDeobfuscator with configEpoch self-healing
- Enhanced PoTokenWebView with dual token system (playerRequest + streamingData)
- Added PlayerDatesStore for player hash date tracking
- Enhanced StatsForNerds with detailed stream/format information
- Added stream client settings in OtherSettings
- Added From30To31Migration for new schema
- Updated Format entity with additional fields

## Fix: Refetch Lyrics Only Selected Type
- Added resetGlobalAttemptForType() in LyricsFetcher.kt to selectively reset one lyrics type's global attempt ID
- onFetchLyricsAgain now calls resetGlobalAttemptForType before nullifying data (was missing, causing fetch to be skipped)
- Refresh button in "no lyrics found" section now resets only the current lyrics type instead of all three

## Fix: LyricsSettingsMenu Layout
- Removed redundant Column wrapper with verticalScroll/weight in ListMenu path
- ListMenu.Menu already provides its own scrollable content area with proper height constraints

## Fix: Playlist/Album limited to 100 songs (gh-661)
- Fixed `getPlaylistNewMode()` to extract `songsContinuation` from `musicPlaylistShelfRenderer.continuations` instead of broken `contents.lastOrNull()?.continuationItemRenderer`
- Fixed `getPlaylistContinuation()` to check all 4 response structures (Metrolist pattern): `sectionListContinuation`, `musicPlaylistShelfContinuation`, `musicShelfContinuation`, `onResponseReceivedActions`
- Added `List<MusicShelfRenderer.Content>.getContinuation()` extension with `@JvmName`
- Changed `PlaylistSongList.kt` to load all continuation pages before updating UI via `LaunchedEffect(browseId)`
- Removed verbose debug `println` calls from HomePage.kt, ChartsPageComplete.kt, RelatedPage.kt, PlaylistPage.kt, YtMusic.kt
- Confirmed: 223/223 songs loaded for test playlist PLT7j9Cmd9zsH9RE3s8elMSqc3AXGET_4q
