Fixed:
- Theme morphM3 is now the default animated gradient in appearance settings.

Added:
- Visualizer button can now be toggled directly from the Player Action Bar Settings Dialog.

Removed:
- Shake to switch music feature (sensor listener, settings UI, ShakeSensitivity enum, related strings).


## 2026-07-13
- Created: app/n_zik/android/playback/services/PlaybackDataStore.kt
- Modified: app/n_zik/android/playback/services/StreamResolver.kt
- Modified: app/n_zik/android/playback/services/PlayerServiceModern.kt
- Fix: StatsForNerds streamClient data now persists across Android cache wipes

## 2026-07-13
- Modified: app/n_zik/android/playback/services/StreamResolver.kt
- Fix: StatsForNerds missing format fields (Size, Loudness, Perceptual Loudness) for downloaded songs
- Added fetchFormatIfMissing() in outer ResolvingDataSource: checks if any Format field is null/incomplete on every playback, fetches via playerResponseForMetadata and HEAD request, upserts merged result into Room DB

## 2026-07-13
- Modified: Slider components across the app
- Fix: Universal slider architecture with SliderControl replacing old CustomSlider
- Fix: Snapping magnetic thresholds dynamically size-based to feel natural on touch screens
- Fix: Re-enabled missing stepSize across several settings (appearance, player, visualizer, volume, etc)

## 2026-07-14
- Modified: Settings menus and sliders (Appearance, Visualizer, Playback, General)
- Fix: Forced integer-only slider dialogs to strip trailing .0 or .5 from legacy saved preferences.
- Fix: Moved Blur Effect out of Audio Effects and into Player Effects within the list-style popup menu.
- UI: Upgraded Visualizer line thickness slider to use the full SliderSettingsEntry component with chevron and dialog.

## 2026-07-14
- Modified: app/n_zik/android/core/database/Converters.kt
- Fix: Prevent "RuntimeException: Tried to marshall a Parcel that contained Binder objects" when saving player queue with large custom covers by purging "artworkData" before Room serialization. Optimized to only run when "artworkData" is present to avoid GC pauses.
