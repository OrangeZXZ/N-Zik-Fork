# Done - 2026-07-06
- refactor: clean up inline hardcoded imports in project codebase
  - Added Rule 11 to `AGENTS.md` strictly forbidding inline package paths for imports
  - Created `refactor_imports_v2.py` python script that uses targeted regex to safely remove inline packages specific to internal app prefixes (`app.n_zik`, `app.it.fast4x`, `it.fast4x`, etc.), while avoiding collisions with Compose or standard Android classes (like `Color`, `Database`).
  - Refactored 102 `.kt` files to extract the package path into file-level `import` statements
  - Applied the same extraction to `discordrpc` and `nextvisualizer` submodules (modifying 4 files)
- ExportCacheDialog - Fix loader & hardcoded strings
  - Fixed InProgressDialog showing "0/0" when total=0 (hides counter text)
  - Replaced 5 hardcoded Toaster.e() strings with R.string resources
  - Replaced hardcoded "Exporting..." with stringResource(R.string.exporting)
  - Added string resources: exporting, export_copy_error, export_failed, export_start_error, export_error
- fix(home-sync): fix concurrent sync kills + filter logic for YT vs local items
  - Added `activeSyncs` counter to prevent `stopSelf()` from killing concurrent syncs
  - Fix `stopSelf()` placement after try/finally (not inside finally)
  - Artist/Album/Playlist filters: `flag && id.startsWith(...)` (both conditions required for YT path)
  - Local path: `hasValidId` → direct fetch by ID (keeps flag false), else → fallback search by name
  - Playlist sync sets `isYoutubePlaylist = true` on successful sync (logo turns red)
  - Notification icon changed from `R.drawable.sync` to `R.drawable.ic_launcher`
  - Added detailed Timber logs: `[TYPE|METHOD]` with ✓/✗, ID, name, thumbnail, songs, URL
- fix(explicit): preserve Explicit badge when fetching album songs
  - `Innertube.SongItem.asMediaItem` now adds `EXPLICIT_PREFIX` to title (was missing, caused badge loss on DB insert)
- fix(album): prevent unnecessary re-fetch on album screen recomposition
  - Changed `LaunchedEffect(Unit)` to `LaunchedEffect(browseId)` so album API fetch only runs once per album, not on every recomposition (e.g. miniplayer close)
- test(lyrics): add unit & Room tests for lyrics refactor
  - Extrait la logique de décision fetch dans LyricsDecisionMaker.kt
  - Refactorisé LyricsFetcher pour déléguer à LyricsDecisionMaker
  - Ajout LyricsDecisionMakerTest (JUnit 5) : karaoke/sync/unsync fetch decisions + fallback BetterLyrics
  - Ajout LyricsTableTest (Robolectric + Room in-memory) : upsert, overwrite, isolation par song
  - Ajout dépendances test : robolectric, room-testing, androidx.test:core, junit-vintage-engine dans libs.versions.toml et build.gradle.kts
- refactor(lyrics): isoler le fallback BetterLyrics Sync dans le slot Karaoke DB
  - BetterLyrics Sync (fallback) stocké dans la colonne KARAOKE pour ne pas polluer la colonne SYNC
  - Empêche le re-fetch inutile lors du changement de mode si déjà en cache DB
  - Fix des erreurs de compilation suite aux ajustements de la logique de refetch
- refactor(lyrics): 3 tables distinctes en base de données pour Karaoke / Sync / Unsync
  - Chaque mode a sa propre ligne en DB (plus de mélange dans une seule ligne)
- fix(lyrics): prevent getBoundingBox crash on stale TextLayoutResult (issues #632 #639)
  - Ajout garde `isLayoutStale` dans `KaraokeLyricsView` pour skip le draw si le texte a changé mais que la recomposition n'est pas encore faite (évite `IllegalArgumentException: offset is out of bounds`)
  - Clamp des index des mots karaoke à la longueur réelle du layout pour éviter les out of bounds
- fix(lyrics): reset list scroll state on lyrics type change
  - Utilisation de `LaunchedEffect(text)` dans `SyncedLyricsView` et `KaraokeLyricsView` pour scroller à 0. L'API `rememberLazyListState(key1=text)` n'existe pas.
- fix(lyrics): fix crash when opening lyrics with a custom cover
  - `getBitmapFromUrl` retourne maintenant `Bitmap?` (nullable) au lieu de throw (IllegalArgumentException/IllegalStateException) si l'URI est local (`content://`) ou réseau down.
  - Wrap les appels dans `LyricsScreen` par des `try/catch` pour un fallback fluide
  - Ajustement des retours nullable et throw d'Exception pour le fallback natif dans `Player`, `MiniPlayer`, et `NextVisualizer`
- fix(album): correctly parse album tracks and clear old cache
  - Extraction des pistes depuis la réponse album directe (`musicShelfRenderer` dans `secondaryContents`) pour garantir l'ordre original au lieu de fetcher la playlist YouTube générée qui est souvent désordonnée.
  - Reset / Clear complet des anciennes lignes dans `SongAlbumMapTable` lors de l'ouverture d'un album pour empêcher les pistes fantômes / anciennement écoutées de fausser l'ordre de l'album.
- fix(ui): prevent modal bottom sheets from drawing behind status bar
  - `CustomModalBottomSheet` instances (main menu, YouTube login, Discord login) now use `modifier = Modifier.statusBarsPadding()` instead of passing `contentWindowInsets`.
  - Removed manual `statusBarHeight` subtraction from `ListMenu` and `GridMenu` height limits, restoring `CONTENT_HEIGHT_FRACTION = 1.0f`.
  - Removed manual drag handle padding hacks in `AccountsSettings` sheets.
  - Set root Column modifier in `YouTubeLogin` and `Discord` composables to `Modifier.fillMaxSize()` so WebViews take full available sheet height.


## 2026-07-05
- fix(home-sync): fix compilation errors in HomeSyncService background refactor
  - Fix browseEndpoint → info.endpoint?.browseId for search result ID extraction
  - Fix YtMusic.getAlbum() return type: access album properties via .album (AlbumPage.album)
  - Remove songCount/duration params not in Album entity
  - Fix it.name null safety with .orEmpty() in joinToString
  - Fix HomeLibrary playlist.id Long→String conversion for putStringArrayListExtra
  - Add missing string resources: sync_no_items, sync_completed_with_errors