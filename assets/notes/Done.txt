# Changelog

## 2026-07-08

### Added
- docs(agent): update AGENTS.md with 3-package tree structure, file placement rules, and dual enforcement
  - Expanded "Project Structure" section with full directory tree for all 3 packages (`app.n_zik.android`, `app.it.fast4x.rimusic`, `app.kreate.android`)
  - Added "Three Package Trees" table with roles, sizes, and rules for each package
  - Added detailed `app.n_zik.android` structure breakdown (components, core, playback, etc.)
  - Added "File Placement (MANDATORY)" section with placement rules and component location table
  - Added "Dual Enforcement: AGENTS.md + BMAD" section — agents must follow BOTH sets of rules in parallel
  - Marked both `app.it.fast4x.rimusic` and `app.kreate.android` as legacy (READ-ONLY, will be removed)

### Added
- feat(audiobar): update waveform ! (gh-605)
  - Renamed "Refresh Fake Audio Bar" to "update waveform !" in `PlayerItemMenu.kt`
  - Reused the logo from the visualizer (playing_indicator)
  - Hidden the button when the fake audio bar is not used
  - Greyed out the button if the song is not 100% cached or downloaded
  - Added a Toaster error when clicking while the song isn't ready
  - Fixed the button staying blocked after a 100% download (dynamic cache check at click-time in `onShortClick`)
  - Added `extractionErrorSignal` to `WaveformExtractor` so the UI knows exactly when an extraction fails due to an incomplete cache instead of timing out silently
  - Fixed the layout and sorting of buttons in `PlayerItemMenu` (ListMenu & GridMenu) to logically group them (Playback, Management, Navigation) matching `SongItemMenu`
  - Removed inline import paths for cleaner code

### Fixed
- fix(menu): reset at 50% without ui lags or buggy animations
- fix(audiobar): preserve Format on download removal and remove debug logs (`078e7c025c`)
  - Removed `formatTable.deleteBySongId()` from `STATE_REMOVING` in `MyDownloadService` so StatsNerds keeps bitrate/size metadata after download deletion
  - Cleaned up all debug Timber logs across audiobar, menus, and settings
- fix(player): resolve missing closing animations on manual dismiss (`9f35d2c8e2`)
  - Wrapped `showPlayer = false` state changes in a coroutine to allow `sheetState.hide()` to complete first.
  - Fixes the bug where clicking the chevron down button caused the player to disappear instantly without animation.
  - Added new Rule 17 to AGENTS.md for `CustomModalBottomSheet` animation guidelines.
- fix(player): revert hide() on video-to-audio switch to prevent getting stuck
  - Switching from Video to Audio leaves the audio sheet state hidden if we force a hide, breaking the toggle.
- fix(menu): close menu on navigation
- fix(lyrics): prevent FOREIGN KEY constraint crash on fast lyric APIs (Heisenbug)
  - Created `saveLyricsSafe` helper in `LyricsFetcher.kt` to catch `SQLiteConstraintException`
  - Implemented a 5-second asynchronous retry delay to allow the background audio player enough time to persist the parent `Song` entity
  - Replaced all raw `Database.asyncTransaction { lyricsTable.upsert(...) }` calls with `saveLyricsSafe(...)`
- fix(lyrics): fix erratic scroll offset for multi-line synced lyrics
  - Removed hardcoded `lineMultiplier` values in `SyncedLyricsView.kt`
  - Standardized the multiplier to `0.35f` for any line count greater than 1
- fix(lyrics): SyncedLyricsView recenter bug fixed by using `animateScrollBy` and exact viewport centering logic identical to `KaraokeLyricsView`
- fix(lyrics): KaraokeLyricsView scroll offset adjusted when background vocals are present below
- fix(lyrics): adjusted line multipliers for 4+ lines in SyncedLyricsView thumbnail mode
- fix(song): GoToAlbum fallback to searchPage API when nextPage fails to find the album
- fix(search): removed explicit `e:` prefix in `SearchSuggestions.kt` to fix UI duplication of the explicit badge in the search suggestions list

---

## 2026-07-07

### Added
- feat(player): crop video thumbnails to 1:1 (issue [#564](https://github.com/N-Zik-Group/N-Zik/issues/564))
  - Added option `cropVideoThumbnailsKey` to force a 1:1 square crop for thumbnails
  - Enabled by default to fix videos fitting weirdly in the 1:1 box

### Fixed
- fix(explicit): fully repaired explicit badge handling across DB, UI, Notifications and Android Auto
  - Moved the 🅴 emoji from the Artist string to the Title string in `SongItem.asMediaItem` and `Song.asMediaItem`
  - Repaired `MediaItem.asSong` to properly reconstruct the internal `e:` prefix if `isExplicit` is true
  - Added explicit injection of `\uD83C\uDD74` into `CustomMediaNotificationProvider` and the custom notification builder
  - Restored the `stripExplicitEmoji()` string extension helper

---

## 2026-07-06

### Added
- feat(home): made Home Quick Picks categories load progressively and asynchronously
  - Removed the global blocking `if (showLoader)` so the UI renders immediately
  - `homePageInit` state now updates incrementally inside the `repeat(3)` fetch loop in `HomeQuickPicksState.kt`
  - Replaced the global `HomeBottomShimmer` with localized placeholder shimmers (empty and partially loaded categories)
  - Fixed pull-to-refresh (`refresh()`) to correctly clear `homePageInit`, `discoverPageInit`, and `chartsPageInit` states

### Fixed
- fix(playback): removed playback delay when clicking songs in lists (issue #633)
  - Refactored `forcePlayAtIndex` in `Player.kt` to start playback immediately via `setMediaItems`/`playWhenReady`
  - Moved the network prefetch (`upsertSongInfo`) to run asynchronously after playback starts
  - Used ExoPlayer's `replaceMediaItem` to update the currently playing `MediaItem` without interrupting playback
- fix(like): fixed download-on-like race condition and UI double-tap bug (issue #609)
  - Replaced the buggy `Database.asyncQuery` race condition with a synchronous `likedAt` check and direct `MyDownloadHelper.downloadOnLike` call
  - Eliminated a memory leak (`currentSong.collect`) triggered every time Like was pressed from the notification
  - Updated `Player.kt` `onDoubleTap` to use `YouTubeSync.toggleSongLike`
- fix: ExportCacheDialog — loader & hardcoded strings
  - Fixed InProgressDialog showing "0/0" when total=0 (hides counter text)
  - Replaced 5 hardcoded `Toaster.e()` strings with `R.string` resources
  - Replaced hardcoded "Exporting..." with `R.string.exporting`
  - Added string resources: `exporting`, `export_copy_error`, `export_failed`, `export_start_error`, `export_error`
- fix(home-sync): fixed concurrent sync kills + filter logic for YT vs local items
  - Added `activeSyncs` counter to prevent `stopSelf()` from killing concurrent syncs
  - Fixed `stopSelf()` placement after try/finally (not inside finally)
  - Artist/Album/Playlist filters: `flag && id.startsWith(...)` (both conditions required for YT path)
  - Local path: `hasValidId` → direct fetch by ID, else fallback search by name
  - Playlist sync sets `isYoutubePlaylist = true` on successful sync (logo turns red)
  - Notification icon changed from `R.drawable.sync` to `R.drawable.ic_launcher`
  - Added detailed Timber logs: `[TYPE|METHOD]` with ✓/✗, ID, name, thumbnail, songs, URL
- fix(lyrics): prevented `getBoundingBox` crash on stale `TextLayoutResult` (issues #632, #639)
  - Added `isLayoutStale` guard in `KaraokeLyricsView` to skip drawing when text changed but recomposition hasn't happened yet
  - Clamped karaoke word indices to the actual layout length to avoid out-of-bounds errors
- fix(lyrics): reset list scroll state on lyrics type change
  - Used `LaunchedEffect(text)` in `SyncedLyricsView` and `KaraokeLyricsView`
- fix(lyrics): fixed crash when opening lyrics with a custom cover
  - `getBitmapFromUrl` now returns a nullable `Bitmap?` instead of throwing for local (`content://`) or offline network URIs
  - Wrapped calls in `LyricsScreen` with try/catch for a smooth fallback
- fix(album): correctly parse album tracks and clear old cache
  - Extract tracks from the direct album response (`musicShelfRenderer` in `secondaryContents`) to preserve the original order
  - Full reset/clear of old rows in `SongAlbumMapTable` when opening an album to prevent ghost/stale tracks
- fix(ui): prevented modal bottom sheets from drawing behind the status bar
  - `CustomModalBottomSheet` instances (main menu, YouTube login, Discord login) now use `Modifier.statusBarsPadding()`
  - Removed manual `statusBarHeight` subtraction from `ListMenu`/`GridMenu` height limits (restored `CONTENT_HEIGHT_FRACTION = 1.0f`)
  - Removed manual drag handle padding hacks in `AccountsSettings` sheets
  - Set root Column modifier in `YouTubeLogin` and `Discord` composables to `Modifier.fillMaxSize()`
  - Replaced hardcoded `700.dp` height constraint in `VideoOrSongInfoScreen` with `Modifier.fillMaxSize()`
- fix(timeline): vz visualizer timeline progress bar no longer resets on crossfade
  - Used `rememberUpdatedState` in `AudioWavePill`

### Refactor
- refactor(visualizer): improved settings menu layout to match the lyrics menu
  - Fixed `BottomSheetMenu` not rendering by adding the `.MenuComponent()` call
  - Replaced manual rows with `SectionTitle`, `ToggleSettingEntry`, `EnumSettingEntry` helpers
  - Fixed scroll, padding, and `navigationBarsPadding`
- refactor: cleaned up inline hardcoded imports across the codebase
  - Added Rule 11 to `AGENTS.md` strictly forbidding inline package paths for imports
  - Created `refactor_imports_v2.py` script (targeted regex, avoids collisions with Compose/Android classes)
  - Refactored 102 `.kt` files to extract package paths into file-level imports
  - Applied the same extraction to the `discordrpc` and `nextvisualizer` submodules (4 files)
- refactor(lyrics): isolated the BetterLyrics Sync fallback in the Karaoke DB slot
  - Stored in the KARAOKE column to avoid polluting the SYNC column
  - Prevents unnecessary re-fetching when switching modes if already cached
- refactor(lyrics): 3 distinct database tables for Karaoke / Sync / Unsync
  - Each mode now has its own DB row

### Other
- test(lyrics): added unit & Room tests for the lyrics refactor
  - Extracted fetch decision logic into `LyricsDecisionMaker.kt`
  - Refactored `LyricsFetcher` to delegate to `LyricsDecisionMaker`
  - Added `LyricsDecisionMakerTest` (JUnit 5): karaoke/sync/unsync fetch decisions + BetterLyrics fallback
  - Added `LyricsTableTest` (Robolectric + Room in-memory): upsert, overwrite, per-song isolation
  - Added test dependencies (robolectric, room-testing, androidx.test:core, junit-vintage-engine)

---

## 2026-07-05

### Fixed
- fix(home-sync): fixed compilation errors in the HomeSyncService background refactor
  - Fixed `browseEndpoint` → `info.endpoint?.browseId` for search result ID extraction
  - Fixed `YtMusic.getAlbum()` return type: access album properties via `.album` (`AlbumPage.album`)
  - Removed `songCount`/`duration` params not present in the `Album` entity
  - Added null safety on `it.name` with `.orEmpty()` in `joinToString`
  - Fixed `playlist.id` Long → String conversion for `putStringArrayListExtra`
  - Added missing string resources: `sync_no_items`, `sync_completed_with_errors`