- Fix bottom floating bar / mini player which hides the content in :
  - ArtistLocalSongs.kt (local tab of the artist page) : added contentPadding = PaddingValues(bottom = Dimensions.bottomSpacer) on both variants (modern + legacy)
  - StatisticsPage.kt : added contentPadding on LazyVerticalGrid + removed the misplaced Spacer (brother of the grid in a Box)
  - Same pattern as ArtistDetails.kt which already worked

- Added an option to enable/disable discord browsing

- Fix YouTube Music metadata bug for songs with combined authors (e.g. "Bebe Rexha & David Guetta" where YTM returns a single run with no navigationEndpoint)
  - Innertube.kt SongItem.parse: removed filter that dropped runs without navigationEndpoint, so parseArtists() can split combined names
  - FromMusicResponsiveListItemRenderer.kt: same fix, replaced filterIsInstance<Browse> with name.isNullOrBlank() check
  - AlbumPage.kt: song.authors?.takeIf { it.isNotEmpty() } ?: album.authors (handles empty list, not just null)
  - Utils.kt parseArtists(): splits combined names on " & " and "," and dedupes case-insensitively

- Fix artist name wipe after play+back to album
  - Database.upsert(SongItem): protect against wipe - if new value is blank/null but DB has a value, keep DB value (for both title and artistsText)
  - Database.upsert(SongItem): search online for missing browse IDs by exact name match (case-insensitive) before falling back to first result
  - Database.insertIgnore(MediaItem): same online search for artists when no browse IDs are provided by YTM
  - Database.insertIgnore(MediaItem): search online now runs in background coroutine (Dispatchers.IO) to not block album load

- Pre-fetch full song metadata (browse IDs) before playback
  - StreamResolver.upsertSongInfo exposed as public
  - Player.forcePlayAtIndex: launches non-blocking pre-fetch + sync read of DB to enrich MediaItem right away
  - UI auto-updates via collectAsState when DB changes

- Fix "More of [artist]" button in song/video/search item menus
  - SongItemMenu.kt, VideoItemMenu.kt, SearchItemMenu.kt: try DB by name first (works after search online populated it), fallback to Innertube nextPage
  - Use NavRoutes.artist.navigateHere() to dispatch navigation on Main thread (fixes crash when called from Dispatchers.IO)

- Fix Database.upsert(SongItem) to be synchronous (runBlocking + withTransaction)
  - Previously used asyncTransaction which was fire-and-forget
  - forcePlayAtIndex now calls upsertSongInfo synchronously, so the search online completes
    before the DB is read, ensuring all browse IDs are available for the MediaItem
  - Fixes songs with multiple artists (e.g. "Say My Name" by David Guetta, Bebe Rexha, J Balvin)
    showing only 1 artist on first play

- ImageCacheFactory.SubcomposeImage composable added for artist thumbnails
  - Uses SubcomposeAsyncImage with a CircularProgressIndicator spinner
  - Shows spinner when URL is null (data not yet available)
  - Shows spinner while loading from network
  - Shows image when successfully loaded
  - Shows fallback only on actual error

- Global fix: ImageCacheFactory default fallback changed from R.drawable.ic_launcher_box to R.drawable.loader
  - Applies to Thumbnail, Painter, AsyncImage methods
  - When image URL is null/empty, the loader image shows instead of the no-image icon
  - Real errors still show ic_launcher_box via the error parameter
  - Affects all images throughout the app (albums, songs, artists, playlists, podcasts)
