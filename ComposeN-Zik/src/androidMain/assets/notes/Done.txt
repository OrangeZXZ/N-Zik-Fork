# Done

## 2026-07-11

### Bump version 7.0.0 → 7.1.0 (versionCode 65 → 66)
- feat(player): Transform PlaybackParamsDialog to menu
- feat(stream): align with Metrolist stream/download/cipher architecture
- feat(logging): add quality tracking logs for network, audio, and image
- fix(player): Fix infinite buffering caused by missing cipher timeout
- fix(player): Fix long click on Rectangular play button in Modern controls
- fix(stream-resolver): fix cipher deobfuscation cancellation buffering loop
- fix(playlist): load all songs from playlists/albums exceeding 100 items (gh-661)
- fix(lyrics): refetch only selected lyrics type and menu layout

### fix(player): Fix infinite buffering caused by missing cipher timeout
- Root cause: CipherWebView.deobfuscateSignature() and transformN() had no timeout, causing coroutine to hang forever when JS silently fails
- Added RequestSlot<T> pattern with requestId to prevent stale callback races (aligned with Metrolist)
- Added withTimeout(15s) on deobfuscateSignature() and transformN()
- Added failAsRendererGone() and throwIfDead() for proper dead WebView handling
- Added CipherRendererGoneException for renderer death detection
- Updated JS callbacks to pass requestId for stale-result guard
- Fixed withinWindow() usage for cooldowns in PlayerConfigStore
- File: CipherWebView.kt (full rewrite aligned on Metrolist)
- File: PlayerConfigStore.kt (cooldown fix)

### feat(player): Transform PlaybackParamsDialog to menu
- Created PlaybackSettingsMenu.kt in app.n_zik.android.components.menu.player
- Menu with sliders for: speed, pitch, duration, volume, blur, bass boost, loudness, volume boost
- List mode: inline sliders
- Grid mode: items that open SliderDialog (like LyricsSettingsMenu)
- Fixed default volume to 100% (1.0f)
- Removed PlaybackParamsDialog from Dialog.kt
- Works for both Essential and Modern controls

### fix(player): Fix long click on Rectangular play button in Modern controls
- Added onLongClick support to CustomElevatedButton
- Rectangular/Default/Square buttons now open playback settings menu on long click
