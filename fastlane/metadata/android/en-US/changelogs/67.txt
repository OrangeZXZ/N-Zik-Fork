Hotfix:
- Fixed albums with more than 100 songs not loading all tracks (gh-661)
- Added pagination support for album songs using same pattern as playlists

Added:
- Playback settings menu accessible via long-press on play button (speed, pitch, volume, bass boost, loudness, etc.)
- Quality tracking logs for network, audio, and image in player transitions
- Waveform cover gradient (M3E Morphing) now default for new installs
- Preferred Stream Client dialog (Android VR / Web Remix) in NetworkSettings
- Stream client descriptions with ToggleListDialog support
- Validation: at least 1 stream client must be enabled
- Reset/Cancel/OK buttons to both stream client dialogs
- ANDROID client to disabled clients list
- Fallback to WEB_REMIX if all clients are disabled
- Warning log when preferred client is disabled
- clearStreamCaches() function (clears formatCache + playbackDataCache + webRemixFailedIds)
- Restart Player button when stream client settings change
- Stream client settings moved from OtherSettings to NetworkSettings
- Conditional menu display (Preferred Client only shows if Web Remix AND Android VR enabled)
- Recommendation toast to re-download songs after changing stream client
- streamClientRestartNeededKey preference for Restart Player state
- Image resizing to max 1000x1000px on save (saveImageToInternalStorage)
- Coil size constraint for local file:// URLs in loadBitmap
- Error notification (Toaster.e()) when import fails

Changed:
- PlaybackParamsDialog replaced by inline PlaybackSettingsMenu (list/grid modes)
- Dialog files reorganized into categorized subdirectories (album, artist, menu, player, playlist, settings, song, tab)
- Default thumbnail roundness changed from 12 to 20
- YouTube login/logout now clears caches and shows Restart Player
- YouTube login toggle no longer clears account info (just disables Innertube)

Fixed:
- Fixed crash on minified build caused by R8 bytecode verification error (VerifyError on kotlin.math.MathKt and GlanceThemeKt)
- Fixed infinite buffering caused by missing timeout in cipher deobfuscation (stream never falling back to other clients)
- Fixed cipher deobfuscation cancellation buffering loop
- Fixed long click on Rectangular play button not opening playback settings in Modern controls
- Fixed playlists/albums exceeding 100 items not loading all songs (gh-661)
- Fixed lyrics refetching all types instead of only the selected one
- Fixed lyrics menu layout issues
- Fixed stream client names (hardcoded instead of translated)
- Fixed preferred client priority in StreamResolver (replaces "last successful client")
- Quality thresholds aligned with YouTube: Low=48kbps, Medium=128kbps, High=AUDIO_QUALITY_HIGH
- Updated getQuality() in StatsForNerds with all itags (Very High, High, Medium, Low)
- Fixed format!! NPE in StatsForNerds (now uses format?.let)
- Fixed RestartAppDialog now shows on top of UI after import (threading fix)
- Fixed "Both" import mode now chains DB -> Settings correctly
- Fixed Player lag when using custom local images from file picker
- Impact: ~16x memory reduction, smooth song changes

Other:
- Aligned stream/download/cipher architecture with Metrolist (RequestSlot, failAsRendererGone, withinWindow cooldowns)
- Updated internal project documentation for AI coding agents
