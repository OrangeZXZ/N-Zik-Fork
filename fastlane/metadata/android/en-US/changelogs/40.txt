Improved:
- To bypass recent YouTube streaming restrictions and avoid long loading times, the player now memorizes the last successful client and prioritizes it for future sessions.

Fixed:
- Fixed a bug where the Github button in OTA screen was redirecting to a 404 page due to a wrong url prefix
- Fixed an issue with playback jumping to next song even when the 'Skip on error' option is disabled. Added more retry attempts before throwing an error.

Refactored:
- Renamed DataspecServices to StreamResolver and restructured the app.n_zik.android.core.playback package
- Cleaned up project namespace from app.kreate.android to app.n_zik.android without affecting the user-facing application ID
