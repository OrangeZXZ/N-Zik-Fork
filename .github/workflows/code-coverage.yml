name: Code Coverage


on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  code-quality: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:

      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          submodules: true # 

 
      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'corretto'


      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3


      - name: Run tests with coverage
        run: |
          chmod +x gradlew
          # <-- CHANGÉ : On utilise le flavor "Full" pour les tests unitaires
          ./gradlew testFullDebugUnitTest jacocoTestReport --no-daemon

      - name: Convert JaCoCo to Cobertura
        run: |
          wget https://raw.githubusercontent.com/rix0rrr/cover2cover/master/cover2cover.py
          # <-- CHANGÉ : Le chemin cible maintenant le dossier "ComposeN-Zik" et non "app"
          python cover2cover.py ComposeN-Zik/build/reports/jacoco/jacocoTestReport/jacocoTestReport.xml ComposeN-Zik/src/androidMain/java > cobertura.xml
          

      - name: Upload coverage report
        if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
        uses: actions/upload-code-coverage@v1
        with:
          file: cobertura.xml
          language: Kotlin
          label: code-coverage/gradle
